charset = utf-8

# Project Rules

- Do not remove, hide, or disable existing functionality, controls, data, visual indicators, or user-facing elements unless the user explicitly asks for that removal.
- If a requested design or layout change appears to require removing any existing behavior or UI element, ask for confirmation before making that change.
- When improving visuals, preserve the current feature set by default and treat removals as separate product decisions.
